# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf
TilePlacements/*.json filter=lfs diff=lfs merge=lfs -text
docs/*.pck filter=lfs diff=lfs merge=lfs -text
docs/*.pck !text !filter !merge !diff
